import React from "react";

const Button = () => {
  return (
    <button
      type="button"
      className={`my-10 py-4 px-6 bg-blue-gradient rounded-[10px] text-[18px] font-poppins text-primary outline-none `}
    >
      Get Started
    </button>
  );
};

export default Button;
